# Extended TypeScript SDK Environment Variables Template
# Copy this file to .env or .env.local and fill in your values
# Environment variables for Extended TypeScript SDK

# ============================================
# Required for Trading Operations
# ============================================

# API Key (obtained from onboarding or account management)
# API key from Extended Exchange onboarding
X10_API_KEY=your_api_key_here

# StarkNet L2 Public Key (hex string starting with 0x)
# L2 public key (hex string, starts with 0x)
X10_PUBLIC_KEY=0x...

# StarkNet L2 Private Key (hex string starting with 0x)
# L2 private key (hex string, starts with 0x)
X10_PRIVATE_KEY=0x...

# Vault ID (your account's vault number)
# Account vault ID (number)
X10_VAULT_ID=123456

# ============================================
# Optional Variables
# ============================================

# Builder ID (optional, for order builders)
# Builder ID for order builders (optional)
X10_BUILDER_ID=

# L1 Private Key (Ethereum private key, required for onboarding only)
# Only needed if you want to create new accounts
# Format: 0x... or without 0x prefix
L1_PRIVATE_KEY=

# Environment (testnet or mainnet, defaults to testnet)
ENVIRONMENT=testnet

# ============================================
# Alternative Naming (also supported by TypeScript SDK)
# ============================================
# The TypeScript SDK also supports these alternative variable names for compatibility:
# API_KEY (instead of X10_API_KEY)
# Stark_Key_Public (instead of X10_PUBLIC_KEY)
# Stark_Key_Private (instead of X10_PRIVATE_KEY)
# Vault_Number (instead of X10_VAULT_ID)
# Client_ID (instead of X10_BUILDER_ID)


